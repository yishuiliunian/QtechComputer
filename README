本系统运行在linux平台之下。在进行编译和调试之前，组要配置一下系统环境。
1、首先安装linux操作系统，这里建议使用ubuntu系统。因为配置简单，最为重要的是他的apt软件管理系统我很喜欢。
2、配置编译环境。主要有安装gcc和nasm编译器。
3、配置程序调试环境。安装bochs和gbd。
4、如果想找个比较号的集成环境的话，不好意思，还真没有呢。这里建议使用vim。用他自己配置环境吧。不懂的地方google一下。
注：整个程序的编译使用make自动编译工具。在Thesis中有设计的一个概要，可以参考。
